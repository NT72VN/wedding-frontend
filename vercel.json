{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist/wedding-frontend",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/.*",
            "dest": "/index.html"
        }
    ]
}