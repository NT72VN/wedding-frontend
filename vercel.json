{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/wedding-frontend/browser",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}